# Core service
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.6

# Tiers 1-2 only (LLM). The service and tests run without it if you only
# need the offline tier — the import is lazy.
litellm>=1.40

# MCP surface (sarathi.mcp_server) — official Model Context Protocol SDK.
mcp>=1.9

# Tests
pytest>=8.0
